server:
  port: 8080

spring:
  application:
    name: HOTELSERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/hotelservice
    username: root
    password: premlata123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  config:
    import: optional:configserver:http://localhost:8085

